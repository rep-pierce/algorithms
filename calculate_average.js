function findAverage(array) {
    let count = 0
    if (array == ''){
      return 0;
    }else {
      for (number of array){
        count = number + count ;
      }return count/array.length
    }
  
  }